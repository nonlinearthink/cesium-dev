{
  "name": "cesium-dev",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm -r --filter \"./packages/cesium-supermap-rest-imagery-provider\" run dev",
    "build": "pnpm -r --filter \"*\" run build"
  },
  "devDependencies": {
    "typescript": "^5.2.2"
  }
}
